package com.flag.app.viewholders;

import android.view.View;
import android.widget.ImageView;
import android.widget.LinearLayout;
import android.widget.TextView;

import androidx.annotation.NonNull;
import androidx.recyclerview.widget.RecyclerView;

import com.flag.app.R;

/*
public class FeedViewHolder extends RecyclerView.ViewHolder {
    public ImageView userIcon;

    public TextView userName, postingTime;

    public FeedViewHolder(@NonNull View itemView) {
        super(itemView);
        userIcon = (ImageView) itemView.findViewById(R.id.user_icon);
        userName = (TextView) itemView.findViewById(R.id.user_name_text);
        postingTime = (TextView) itemView.findViewById(R.id.posting_time_text);
    }

    public ImageView getUserIcon() {
        return userIcon;
    }

    public TextView getUserName() {
        return userName;
    }

    public TextView getPostingTime() {
        return postingTime;
    }
}
 */
