package com.flag.app.adapters;
/*

import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import androidx.annotation.NonNull;
import androidx.recyclerview.widget.RecyclerView;

import com.flag.app.R;
import com.flag.app.model.FeedItem;
import com.flag.app.viewholders.FeedViewHolder;
 */

import java.util.List;

//public class FeedAdapter extends RecyclerView.Adapter<FeedViewHolder> {
    /*
    private List<FeedItem> listFeed;

    public FeedAdapter(List<Feed> listFeed) {
        this.listFeed = listFeed;
    }


    @Override
    public int getItemViewType(final int position) {
        return R.layout.feed_item;
    }

    @NonNull
    @Override
    public FeedViewHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {
        View view = LayoutInflater.from(parent.getContext()).inflate(viewType, parent, false);
        return new FeedViewHolder(view);    }

    @Override
    public void onBindViewHolder(@NonNull FeedViewHolder holder, int position) {
        Feed feed = listFeed.get(position);
        holder.getUserName().setText(String.valueOf(feed.getUserName()));
        holder.getPostingTime().setText(String.valueOf(feed.getPostingTime()));
    }

    @Override
    public int getItemCount() {
        return listFeed.size();
    }
     */
//}
